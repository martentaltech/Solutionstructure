@page "/countrycurrencies/delete"
@attribute [ExcludeFromInteractiveRouting]
@inject ICountryCurrenciesRepo repo
@inject NavigationManager NavigationManager

<MyDeletePage PageTitleText="Delete Country Currency" EntityTitle="Country Currency" Entity="item" BasePath="/countrycurrencies" OnValidSubmit="delete" />

@code {
    private CountryCurrency item;
    [SupplyParameterFromQuery] private Guid Id { get; set; }
    protected override async Task OnInitializedAsync() {
        item = await repo.GetAsync(Id);
        if (item is null) NavigationManager.NavigateTo("/countrycurrencies");
    }
    private async Task delete() {
        await repo.DeleteAsync(item!.Id);
        NavigationManager.NavigateTo("/countrycurrencies");
    }
}
